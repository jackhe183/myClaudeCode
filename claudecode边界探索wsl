在安全工程和自动化开发中，这叫做**“环境探针（Environment Profiling）”**或者**“边界测试”**。

因为你现在的运行环境非常特殊且叠了好几层 BUFF：
1. **系统层**：精简版的 Alpine Linux（很多标准的 Ubuntu 命令它没有）。
2. **权限层**：你是最高权限的 `root` 用户。
3. **文件层**：你跨界挂载了 Windows 的 C 盘（`/mnt/host/c/...`）。
4. **模型层**：你使用的是第三方/国产模型（有些模型的工具调用能力、上下文长度可能和官方原版有差异）。

让 Claude 自己去探明它的**“手脚有多长、力气有多大、哪里是雷区”**，对你后续让他干活至关重要。

请复制下面这段精心设计的**“边界探索提示词”**，直接发送给 WSL 里的 Claude Code：

***

### 复制以下提示词发送给 Claude Code：

```text
你好！你现在运行在一个基于 CLI 的自动化代码助手（Claude Code）环境中。我的最终目标是让你协助我进行代码开发、文件处理和系统操作。

但在开始具体工作之前，我需要你对自己当前所处的操作系统环境、权限边界、可用工具链以及网络限制进行一次**全面、安全的自我探索**。

请调用你的 Bash 执行工具和文件系统工具，执行以下 5 项探索任务，并在完成后给我输出一份详细的《当前运行环境与能力边界评估报告》。

**⚠️ 探索原则：绝对安全！只允许执行只读命令，或者在当前目录下创建/删除临时测试文件，绝不允许修改或删除任何现有的系统文件和用户文件！**

---
**【任务 1：系统与内核探勘】**
- 你现在处于什么具体的 Linux 发行版中？（提示：查看 /etc/os-release）
- 系统的内核版本和架构是什么？
- 你当前的用户是谁？（whoami）具备哪些系统权限？

**【任务 2：工具链与包管理器摸底】**
- 检查当前系统默认的包管理器是什么？（测试 apt, apk, yum, pacman 等哪个可用）
- 检查以下核心开发工具是否已安装及版本：git, curl, wget, python3, node, gcc, make。
- 如果缺少上述某个重要工具，你是否有权限且知道如何使用包管理器安装它？

**【任务 3：文件系统与跨系统读写边界】**
- 你当前所在的绝对路径是什么？
- 请在当前目录下创建一个名为 `claude_test_probe.txt` 的文件，写入一段测试文本，读取它，然后删除它。以此证明你的完整读写权限。
- 请检查你是否能访问 `/mnt/host/c/`（这通常是挂载的宿主机 Windows 驱动器）？你对这个跨界挂载目录的文件有读取权限吗？有写入权限吗？

**【任务 4：网络通信边界】**
- 请尝试使用 curl 或 ping 访问一个公共网站（如 github.com 或 baidu.com），测试你是否具备直接访问外网的能力？

**【任务 5：你自身的内部 Tool 能力审计】**
- 作为 Claude Code，你的底层框架为你注入了哪些原生工具（Tools）？（例如：Bash执行、文件读写、全局搜索 Glob、代码分析等）
- 结合你正在使用的底层 LLM 模型的实际表现，你在连续调用多个工具时，是否稳定？

---
**探索完成后，请以 Markdown 格式输出你的《环境与边界报告》，报告必须包含以下三个核心板块：**
1. **我能做什么（Capabilities）**：你确认为可用的工具、权限和操作范围。
2. **我的受限区（Limitations & Blockers）**：你发现缺失的命令、没有权限的目录、网络限制，或者因为是精简版系统（如 Alpine）导致的特殊限制。
3. **给主人的建议（Recommendations）**：为了让你更好地帮我写代码，你建议我（用户）在这个环境里提前安装什么，或者注意什么。

开始执行吧，把你的每一步探索过程和思考都展示出来！
```

***

### 这段提示词的精妙之处在于：
1. **防止破坏**：明确规定了“绝对安全”原则，防止它为了测试权限把你的系统文件搞乱。
2. **摸清底牌**：它会去探测 `apk`（Alpine 的包管理器），如果它自己发现了，以后它缺少什么工具就会**自动用 apk 去装**，不需要再麻烦你。
3. **验证跨系统能力**：特意让它去摸索 `/mnt/host/c/`，测试它操作你 Windows 文件的极限，看看有没有权限坑。
4. **测试模型智商**：这个任务包含多步逻辑和连续的工具调用（执行命令 -> 分析结果 -> 再次执行）。这能**极大地考验你当前使用的国产模型是否聪明**。如果模型能力弱，可能会在中间卡住或忘记任务；如果模型优秀，它会像一个真正的黑客一样一步步完成勘探并给你完美的报告。

把这段发给它，看看它会给你交出一份怎样的“体检报告”吧！如果在探索中它卡住了，随时告诉我！
